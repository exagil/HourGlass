<div class="time_form">
		
	<div class="quote-banner">
		<h3>At what time did you come today?</h3>
	</div>

	<%= form_for(@todays_activity, html: {class: 'todays_time'}, remote: true) do |f| %>
		<%= f.text_field :start_time %>
		<div class="btn-group">
		<%= f.submit "Check In", class: 'button' %>
		<%= f.submit "Check In", class: 'button' %>
		</div>
	<% end %>

</div>